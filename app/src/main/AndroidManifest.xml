<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comedor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/splash1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.ComedorView.ComedorCheckReports"></activity>
        <activity android:name=".View.ComedorView.CheckPersonalView" />
        <activity android:name=".View.ComedorView.AddConsumoView" />
        <activity android:name=".View.ComedorView.ServiceActivity" />
        <activity android:name=".View.ComedorView.ComedorInfoView" />
        <activity android:name=".View.ComedorView.ServicesInfoView" />
        <activity android:name=".View.ComedorView.ComedorActivity" />
        <activity android:name=".View.AdministratorView.AdministratorActivity" />
        <activity android:name=".View.RecoverView.ForgotPassActivity" />
        <activity android:name=".View.AdministratorView.ReporterActivity" />
        <activity android:name=".View.RegisterView.RegistrarActivity" />
        <activity android:name=".View.LoginView.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>